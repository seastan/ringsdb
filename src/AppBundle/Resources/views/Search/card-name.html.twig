{% if card.is_unique == true %}<span class="icon-unique" title="Unique" data-toggle="tooltip" data-container="body"></span>{% endif %}
<span class="card-name{% if card.available == false %} card-preview{% endif %}">{{ card.name }}</span>
