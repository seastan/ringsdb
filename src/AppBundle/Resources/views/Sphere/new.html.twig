{% extends 'AppBundle:Admin:base.html.twig' %}

{% block content -%}
<h1>Sphere creation</h1>

    <form action="{{ path('admin_sphere_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Create</button>
        <a href="{{ path('admin_sphere') }}" class="btn btn-default">
         	Back to the list
     	</a>
    </form>
{% endblock %}
