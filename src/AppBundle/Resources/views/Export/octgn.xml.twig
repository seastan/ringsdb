<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<deck game="a21af4e8-be4b-4cda-a6b6-534f9717391f">
  <section name="Hero" shared="False">
{% for slot in deck.slots_by_type.contract %}
{% if slot.card.cost == '' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type.hero %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endfor %}
  </section>
  <section name="Ally" shared="False">
{% for slot in deck.slots_by_type.ally %}
{% if slot.card.cost != '-' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type.treasure %}
{% if slot.card.name == 'Berelind' or slot.card.name == 'Coruhaer' or slot.card.name == 'Laermuin' or slot.card.name == 'Elrond\'s Champion' or slot.card.name == 'Rivendell Skirmishers' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
  </section>
  <section name="Attachment" shared="False">
{% for slot in deck.slots_by_type.attachment %}
{% if slot.card.cost != '-' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type.contract %}
{% if slot.card.cost != '' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type.treasure %}
{% if slot.card.cost != '' and slot.card.name != 'Brace of Coneys' and slot.card.name != 'Ho! Tom Bombadil!' and slot.card.name != 'Mendor\'s Support' and slot.card.name != '"No Time to Waste!"' and slot.card.name != 'Ranger Sense' and slot.card.name != 'Determination' and slot.card.name != 'Lore of Ancient Arnor' and slot.card.name != 'Berelind' and slot.card.name != 'Coruhaer' and slot.card.name != 'Laermuin' and slot.card.name != 'Elrond\'s Champion' and slot.card.name != 'Rivendell Skirmishers' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
  </section>
  <section name="Event" shared="False">
{% for slot in deck.slots_by_type.event %}
{% if slot.card.cost != '-' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type.treasure %}
{% if slot.card.name == 'Brace of Coneys' or slot.card.name == 'Ho! Tom Bombadil!' or slot.card.name == 'Mendor\'s Support' or slot.card.name == '"No Time to Waste!"' or slot.card.name == 'Ranger Sense' or slot.card.name == 'Determination' or slot.card.name == 'Lore of Ancient Arnor' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
  </section>
  <section name="Side Quest" shared="False">
{% for slot in deck.slots_by_type['player-side-quest'] %}
{% if slot.card.cost != '-' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
  </section>
  <section name="Sideboard" shared="False">
{% for group in deck.sideslots_by_type %}
{% for slot in group %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endfor %}
{% endfor %}
{% for slot in deck.slots_by_type.ally %}
{% if slot.card.cost == '-' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type.attachment %}
{% if slot.card.cost == '-' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type.event %}
{% if slot.card.cost == '-' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type['player-side-quest'] %}
{% if slot.card.cost == '-' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
{% for slot in deck.slots_by_type.treasure %}
{% if slot.card.cost == '' %}
    <card qty="{{ slot.quantity }}" id="{{ slot.card.octgnid }}">{{ slot.card.name }}</card>
{% endif %}
{% endfor %}
  </section>
  <section name="Quest" shared="True" />
  <section name="Encounter" shared="True" />
  <section name="Special" shared="True" />
  <section name="Setup" shared="True" />
  <notes><![CDATA[]]></notes>
</deck>
